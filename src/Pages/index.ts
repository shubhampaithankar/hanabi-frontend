import { lazy } from 'react';

export const HomePage = lazy(() => import('./Homepage'));
export const FormPage = lazy(() => import('./FormPage'));
export const ResultPage = lazy(() => import('./ResultPage'));